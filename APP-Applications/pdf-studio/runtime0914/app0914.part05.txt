viewPref0914(),preview=p.preview||p.mode==='preview';E.queue.classList.toggle('compact0914',p.mode==='compact');qa14('.fileRow',E.queue).forEach(row=>{row.classList.toggle('preview0914',preview);if(preview){const i=+row.dataset.fileIndex;thumb0914(row,S.files[i])}})}
const baseQueue0914=queue;queue=function(){baseQueue0914();installFileView0914();decorateFiles0914()};

// ---------------- Output templates and clearer modes ----------------
function dateContext0914(v){const d=new Date(`${v||today()}T12:00:00`),base=dc(v),mon=MONTHS[d.getMonth()]||'',short=mon.slice(0,4);return{...base,YYYY:String(d.getFullYear()),YY:String(d.getFullYear()).slice(-2),MM:String(d.getMonth()+1).padStart(2,'0'),M:String(d.getMonth()+1),MMM:short,MMMM:mon,DD:String(d.getDate()).padStart(2,'0'),D:String(d.getDate()),WW:base.WEEK_PAD,W:base.WEEK}}
const baseOutName0914=outName;
outName=function(name=S.workingName||current()?.name||'document.pdf'){const n=split(name),rel=relativeSourceDir(),parts=rel.split('/').filter(Boolean),c={...dateContext0914(E.date.value),FILENAME:safe(n.stem),STEM:safe(n.stem),EXT:'pdf',PARENT:safe(parts.at(-1)||''),SOURCE_DIR:safe(rel.replace(/\//g,'-')),...cf(),...operationContext()};let raw=E.tpl.value;raw=raw.replace(/\{DATE(?:\||:)([^}]+)\}/g,(_,pat)=>fmtDate0912(E.date.value,pat));let rendered=safe(raw.replace(/\{([A-Z0-9_]+)\}/g,(_,k)=>c[k]??`{${k}}`));const sx=operationSuffix();if(sx&&!rendered.toUpperCase().includes(sx.toUpperCase()))rendered=rendered.replace(/\.pdf$/i,'')+sx+'.pdf';return rendered};
function insertTplToken0914(v){const i=$14('filenameTemplate');if(!i)return;i.setRangeText(v,i.selectionStart??i.value.length,i.selectionEnd??i.value.length,'end');i.dispatchEvent(new Event('input',{bubbles:true}));updatePath();i.focus()}
function enhanceOutput0914(){const tpl=$14('filenameTemplate');if(!tpl||$14('outputGuide0914'))return;const box=document.createElement('div');box.id='outputGuide0914';box.className='outputGuide0914';box.innerHTML=`<b>Modes de sortie</b><br><b>1 · Chronologique</b> : construit des dossiers par date selon la structure programmable.<br><b>2 · À côté du source</b> : enregistre au même niveau que le fichier d'origine lorsque le navigateur l'autorise.<br><b>3 · Miroir</b> : crée un dossier d'opération et conserve l'arborescence relative.<br><b>4 · Téléchargements</b> : sortie navigateur, recommandée pour la démo et le web public.<details><summary>Construire le nom du fichier</summary><div class="templateTokens0914">${['{FILENAME}','{DATE}','{DATE:YYYYMMDD}','{YYYY}','{MM}','{MMMM}','{WW}','{INITIALS}','{CLIENT}','{PARENT}','{SOURCE_DIR}'].map(x=>`<button type="button" data-tpl="${x}">${x}</button>`).join('')}</div><div class="nlab0914Help">Exemples : <code>{FILENAME}_{DATE:YYYYMMDD}.pdf</code> · <code>{CLIENT}_{FILENAME}_{MMMM}_{YYYY}.pdf</code>. Les suffixes d'opérations et de tampons sont ajoutés après ce modèle lorsqu'ils sont activés.</div></details>`;tpl.closest('.field')?.after(box);box.querySelectorAll('[data-tpl]').forEach(b=>b.onclick=()=>insertTplToken0914(b.dataset.tpl))}

// ---------------- Redaction planning / final apply ----------------
const baseNewRedaction0914=newRedaction;
newRedaction=function(x=70,top=80){baseNewRedaction0914(x,top);S.editorTool='select';syncEditorToolbar();renderEditorOptions();updateRedactionPlan0914();st('Zone de caviardage préparée : déplacez/redimensionnez-la, ajoutez d’autres zones, puis appliquez définitivement.')};
function updateRedactionPlan0914(){const b=$14('redactionCount0914');if(b)b.textContent=`${(S.annotations||[]).filter(a=>a.redaction).length} zone(s) préparée(s)`}
function enhanceRedaction0914(){const dpi=E.form?.querySelector('[name=redactionDpi]');if(!dpi||$14('redactionPlan0914'))return;const box=document.createElement('div');box.id='redactionPlan0914';box.className='redactionPlan0914';box.innerHTML='<b>Préparation du caviardage</b> · <span id="redactionCount0914" class="redactionCount0914">0 zone</span><div class="row"><button id="redAdd0914" type="button">＋ Ajouter une zone</button><button id="redSelect0914" type="button">↔ Sélectionner / déplacer</button><button id="redApply0914" type="button" class="danger">■ Appliquer définitivement</button></div><div class="nlab0914Help">Les rectangles noirs restent des objets déplaçables/redimensionnables tant que vous n’avez pas cliqué « Appliquer définitivement ». L’application finale rasterise les pages concernées afin de supprimer le contenu sous les zones. Détection automatique de données personnelles : à développer séparément.</div>';dpi.closest('.field,div')?.after(box);$14('redAdd0914').onclick=()=>newRedaction();$14('redSelect0914').onclick=()=>setEditorTool('select');$14('redApply0914').onclick=()=>run(applyRedactions);updateRedactionPlan0914()}

// ---------------- Signature Google gate visible in the module ----------------
function enhanceSignature0914(){const target=E.form?.querySelector('[name=signatureImageFile], [name=savedSignature], .digitalSignatureBox');if(!target||$14('signatureGate0914'))return;const configured=!!window.NLAB_GOOGLE_DRIVE_CONFIG?.clientId,box=document.createElement('div');box.id='signatureGate0914';box.className='signatureGate0914';box.innerHTML=`<b>🔐 Espace personnel Google requis</b><div>${configured?'OAuth configuré : connectez votre Drive privé pour utiliser signatures/paraphes.':'OAuth Google n’est pas encore configuré sur cette publication. Les signatures/paraphes restent verrouillés.'}</div><div class="row"><button id="sigGoogleConnect0914" type="button" ${configured?'':'disabled'}>${configured?'Connecter Google Drive':'Configuration OAuth requise'}</button></div>`;target.closest('.field,.signatureImportBox,.digitalSignatureBox,div')?.before(box);$14('sigGoogleConnect0914')?.addEventListener('click',()=>{$14('googleDriveConnect')?.click()})}

// ---------------- Contextual enhancement observer ----------------
function enhanceForm0914(){try{enhanceStamp0914();enhanceHighlight0914();enhancePen0914();enhanceImage0914();enhanceRedaction0914();enhanceSignature0914();enhanceOutput0914();installFileView0914();decorateFiles0914();if(S.editorTool==='text'&&S.selectedAnn?.type==='text')renderEditorOptions()}catch(e){console.warn('0914 enhance',e)}}
const obs=new MutationObserver(()=>setTimeout(enhanceForm0914,0));if(E.form)obs.observe(E.form,{childList:true,subtree:true});

// Version labels and help
function install0914(){document.title='nLab PDF Studio — '+VERSION;document.querySelectorAll('.buildBadge strong').forEach(x=>x.textContent='Alpha 0.9.14 TEST');document.querySelectorAll('.demoChip').forEach(x=>x.textContent=(x.textContent||'').replace(/Alpha 0\.9\.(12|13)( TEST)?/g,'Alpha 0.9.14 TEST'));const foot=document.querySelector('footer .footerInfo span');if(foot)foot.textContent=(foot.textContent||'').replace(/Alpha 0\.9\.(12|13)( TEST)?/g,'Alpha 0.9.14 TEST');const help=$14('editorHelpToggle');if(help)help.title='Aide contextuelle · 0.9.14 : texte enrichi, surlignage texte, rotation image, c