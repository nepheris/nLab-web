e0914(styles){const a=S.selectedAnn;if(!a||a.type!=='text')return toast('Sélectionnez un bloc texte');const el=textDom0914(a);if(!el)return;const scope=$14('richScope0914')?.value||'selection',r=rangeFor0914(scope);if(!r)return;const sp=document.createElement('span');for(const[k,v]of Object.entries(styles))if(v!=null)sp.style[k]=v;try{sp.appendChild(r.extractContents());r.insertNode(sp);el.normalize();commitRichFromDom0914(a,el);commitAnnotationHistory('Formatage partiel du texte');toast(scope==='selection'?'Format appliqué à la sélection':'Format appliqué au bloc')}catch(e){console.warn(e);toast('Sélection complexe : réessayez sur un passage plus court')}}
function clearRich0914(){const a=S.selectedAnn,el=textDom0914(a);if(!a||!el)return;const scope=$14('richScope0914')?.value||'selection',r=rangeFor0914(scope);if(scope==='selection'&&r&&!r.collapsed){const txt=r.toString();r.deleteContents();r.insertNode(document.createTextNode(txt));el.normalize()}else{el.innerHTML=safeHtml14(el.innerText).replace(/\n/g,'<br>')}commitRichFromDom0914(a,el);commitAnnotationHistory(scope==='selection'?'Suppression formatage sélection':'Suppression formatage enrichi');toast(scope==='selection'?'Formatage retiré de la sélection':'Formatage enrichi supprimé')}
function richControlsHtml0914(){const a=S.selectedAnn;if(!a||a.type!=='text')return'';return `<span id="richText0914" class="richText0914"><span class="richRasterBadge0914" title="Le texte à styles mixtes est aplati en image lors de l'export afin de conserver son apparence">Texte enrichi</span><select id="richScope0914" title="Portée"><option value="selection">Sélection</option><option value="all">Tout le bloc</option></select><select id="richFont0914" title="Police de la sélection"><option value="Arial">Arial / Helvetica</option><option value="Times New Roman">Times New Roman</option><option value="Courier New">Courier New</option><option value="Georgia">Georgia</option><option value="Verdana">Verdana</option><option value="Trebuchet MS">Trebuchet MS</option></select><input id="richSize0914" class="size" type="number" min="6" max="96" value="${Math.round(a.size||12)}" title="Taille en points"><button id="richBold0914" type="button"><b>G</b></button><button id="richItalic0914" type="button"><i>I</i></button><button id="richUnderline0914" type="button"><u>S</u></button><button id="richClear0914" type="button" title="Retirer les styles sur la sélection ou le bloc">Normal</button></span>`}
function bindRichControls0914(){const a=S.selectedAnn;if(!a||a.type!=='text')return;$14('richFont0914')?.addEventListener('change',e=>applyRichStyle0914({fontFamily:e.target.value}));$14('richSize0914')?.addEventListener('change',e=>{const base=Math.max(6,+a.size||12),v=Math.max(6,Math.min(96,+e.target.value||base));applyRichStyle0914({fontSize:(v/base).toFixed(3)+'em'})});$14('richBold0914')?.addEventListener('click',()=>applyRichStyle0914({fontWeight:'700'}));$14('richItalic0914')?.addEventListener('click',()=>applyRichStyle0914({fontStyle:'italic'}));$14('richUnderline0914')?.addEventListener('click',()=>applyRichStyle0914({textDecoration:'underline'}));$14('richClear0914')?.addEventListener('click',clearRich0914)}
const baseRenderOptions0914=renderEditorOptions;
renderEditorOptions=function(){baseRenderOptions0914();if(S.editorTool==='text'&&S.selectedAnn?.type==='text'){const bar=$14('editorOptionsBar');if(bar&&!$14('richText0914')){bar.insertAdjacentHTML('beforeend',richControlsHtml0914());bindRichControls0914()}}};
const baseRenderAnns0914=renderAnns;
renderAnns=function(){baseRenderAnns0914();for(const a of S.annotations||[]){if(a.type!=='text')continue;const el=textDom0914(a);if(!el)continue;if(a.richHtml){el.innerHTML=cleanRichHtml0914(a.richHtml)}el.onpaste=e=>{e.preventDefault();const txt=e.clipboardData?.getData('text/plain')||'';document.execCommand('insertText',false,txt)};el.oninput=()=>commitRichFromDom0914(a,el);el.onblur=()=>{commitRichFromDom0914(a,el);commitAnnotationHistory('Édition texte enrichi')}};updateRedactionPlan0914()};
const baseNewText0914=newTextAnn;
newTextAnn=function(x=60,top=70){baseNewText0914(x,top);const a=S.selectedAnn;if(a?.type==='text'){a.richHtml='';a.richVersion=1}};
async function richPng0914(a){const boxW=Math.max(80,Math.min(a.textBoxWidth||260,700));const root=document.createElement('div');root.setAttribute('xmlns','http://www.w3.org/1999/xhtml');root.style.cssText=`box-sizing:border-box;width:${boxW}px;font-family:${a.font==='Times'?'Times New Roman':a.font==='Courier'?'Courier New':'Arial'};font-size:${a.size||12}px;line-height:1.2;color:${a.color||'#0057B8'};font-weight:${a.bold?'700':'400'};font-style:${a.italic?'italic':'normal'};text-decoration:${a.underline?'underline':'none'};text-align:${a.align||'left'};white-space:pre-wrap;overflow-wrap:anywhere;background:transparent;`;
root.innerHTML=cleanRichHtml0914(a.richHtml||safeHtml14(a.text||'').replace(/\n/g,'<br>'));root.style.position='fixed';root.style.left='-10000px';root.style.top='0';document.body.appendChild(root);const h=Math.max((a.size||12)*1.4,Math.ceil(root.scrollHeight+3));root.style.height=h+'px';root.style.position='static';const xml=new XMLSerializer().serializeToString(root);root.remove();const scale=2,svg=`<svg xmlns="http://www.w3.org/2000/svg" width="${boxW*scale}" height="${h*scale}" viewBox="0 0 ${boxW} ${h}"><foreignObject x="0" y="0" width="${boxW}" height="${h}">${xml}</foreignObject></svg>`;const blob=new Blob([svg],{type:'image/svg+xml'}),url=URL.createObjectURL(blob);try{const im=await imageFromUrl(url),cv=document.createElement('canvas');cv.width=Math.ceil(boxW*scale);cv.height=Math.ceil(h*scale);const cx=cv.getContext('2d');cx.clearRect(0,0,cv.width,cv.height);cx.drawImage(im,0,0,cv.width,cv.height);const png=await new Promise((res,rej)=>cv.toBlob(b=>b?res(b):rej(new Error('Rasterisation texte enrichi impossible')),'image/png'));return{bytes:new Uint8Array(await png.arrayBuffer()),width:boxW,height:h}}finally{URL.revokeObjectURL(url)}}
const baseAnnotated0914=annotated;
annotated=async function(){const rich=(S.annotations||[]).filter(a=>a.type==='text'&&a.richHtml);if(!rich.length)return baseAnnotated0914();const all=S.annotations;let bytes;try{S.annotations=all.filter(a=>!rich.includes(a));bytes=await baseAnnotated0914()}finally{S.annotations=all}const doc=await PDFLib.PDFDocument.load(bytes);for(const a of rich){const p=doc.getPages()[a.page-1];if(!p)continue;const r=await richPng0914(a),im=await doc.embedPng(r.bytes),ph=p.getHeight();p.drawImage(im,{x:a.x,y:Math.max(0,ph-a.top-r.height),width:r.width,height:r.height})}return doc.save()};

// ---------------- Structured history ----------------
S.historyMeta0914=S.historyMeta0914||new Map();S.docSeq0914=S.docSeq0914||0;S.annSeq0914=S.annSeq0914||0;
const baseResetHist0914=resetAnnotationHistory;
resetAnnotationHistory=function(){baseResetHist0914();S.historyMeta0914=new Map();S.docSeq0914=0;S.annSeq0914=0;const snap=S.undoStack[0];if(snap)S.historyMeta0914.set(snap