e utilisées dans le texte, le préfixe et le suffixe.</div>`;builder.appendChild(div);div.querySelectorAll('[data-v]').forEach(b=>b.onclick=()=>{const ta=$14('stampBuildTemplate');if(ta){ta.setRangeText(b.dataset.v,ta.selectionStart,ta.selectionEnd,'end');ta.focus()}})}
const add=$14('stampBuildAdd');if(add&&!add.dataset.v0914){add.dataset.v0914='1';add.onclick=()=>{const label=($14('stampBuildLabel')?.value||'Tampon personnalisé').trim(),template=($14('stampBuildTemplate')?.value||'').trim();if(!template)return toast('Saisissez un modèle');const id='custom-'+Date.now(),d={id,category:'Personnalisés',label,template,prefix:$14('stampPrefixTemplate0914')?.value||'',suffix:$14('stampSuffixTemplate0914')?.value||'',prefixEnabled:!!$14('stampUsePrefix0914')?.checked,suffixEnabled:!!$14('stampUseSuffix0914')?.checked,textPrefix:$14('stampTextPrefixFull0914')?.value||$14('stampTextPrefix0912')?.value||'',textSuffix:$14('stampTextSuffix0914')?.value||''};S.stampLibrary.push(d);persistCustomStamps();const c=E.form.querySelector('[name=stampCategory]');if(c)c.dataset.ready='';renderStampLibrary();if(c){c.value='Personnalisés';renderStampLibrary()}const p=E.form.querySelector('[name=preset]');if(p)p.value=id;updateStampPreview();toast(`Tampon « ${label} » ajouté`)}}
}
const baseNewStamp0914=newStampAnn;
newStampAnn=function(x=60,top=70){baseNewStamp0914(x,top);const a=S.selectedAnn,d=stampDefinition(),ds=stampDatesAll0914();if(a?.type==='stamp'){a.stampDate=ds.stamp;a.stampDateA=ds.a;a.stampDateB=ds.b;a.stampDateC=ds.c;a.filePrefix=(d?.prefixEnabled===false?'':resolveStampTemplate(d?.prefix||'',ds.stamp));a.fileSuffix=(d?.suffixEnabled===false?'':resolveStampTemplate(d?.suffix||a.fileSuffix||'',ds.stamp));if(d?.textPrefix)a.text=resolveStampTemplate(d.textPrefix,ds.stamp)+a.text;if(d?.textSuffix)a.text+=resolveStampTemplate(d.textSuffix,ds.stamp);a.dateFormats=formatStore0914();renderAnns()}};

// ---------------- Highlighter presets + real text highlight entry point ----------------
const HI14=[['Jaune','#FFEB3B'],['Vert','#66BB6A'],['Bleu','#42A5F5'],['Rose','#EC407A'],['Orange','#FFA726'],['Rouge','#EF5350']];
const HI14_CUSTOM_KEY=(PROFILE.storagePrefix||'nlab-pdf')+'-highlight-quick-custom-0914';
function hiCustom0914(){try{const x=JSON.parse(localStorage.getItem(HI14_CUSTOM_KEY)||'[]');return Array.isArray(x)?x.filter(c=>/^#[0-9A-F]{6}$/i.test(c)).map(c=>c.toUpperCase()).slice(0,6):[]}catch{return[]}}
function saveHiCustom0914(v){try{localStorage.setItem(HI14_CUSTOM_KEY,JSON.stringify(v.slice(0,6)))}catch{}}
function chooseHighlight0914(picker,color,box){picker.value=color;picker.dispatchEvent(new Event('input',{bubbles:true}));syncSelected();box.querySelectorAll('[data-c]').forEach(x=>x.classList.toggle('active',x.dataset.c.toUpperCase()===color.toUpperCase()))}
function renderHighlightSwatches0914(box,picker){const holder=q14('.highlightPresets0914',box);if(!holder)return;const custom=hiCustom0914();holder.innerHTML=HI14.map(([n,c])=>`<button class="highlightPreset0914" data-c="${c}" data-fixed="1" title="${n}" style="background:${c}"></button>`).join('')+custom.map(c=>`<button class="highlightPreset0914" data-c="${c}" data-custom="1" title="Personnalisé ${c} · Maj+clic pour supprimer" style="background:${c}"></button>`).join('')+'<button id="highlightPlus0914" type="button" title="Ajouter une couleur personnalisée">＋</button>';holder.querySelectorAll('[data-c]').forEach(b=>b.onclick=e=>{const c=b.dataset.c.toUpperCase();if(e.shiftKey&&b.dataset.custom){saveHiCustom0914(custom.filter(x=>x!==c));renderHighlightSwatches0914(box,picker);return}chooseHighlight0914(picker,c,box)});$14('highlightPlus0914').onclick=()=>{const onChange=()=>{const c=String(picker.value||'').toUpperCase();let v=hiCustom0914();if(!HI14.some(x=>x[1]===c)&&!v.includes(c)){v=[...v,c].slice(-6);saveHiCustom0914(v)}renderHighlightSwatches0914(box,picker);chooseHighlight0914(picker,c,box)};picker.addEventListener('change',onChange,{once:true});picker.click()}}
function enhanceHighlight0914(){const picker=E.form?.querySelector('[name=highlightColor]');if(!picker||$14('highlightPresets0914'))return;const box=document.createElement('div');box.id='highlightPresets0914';box.innerHTML='<div class="highlightMode0914"><button id="highlightZone0914" type="button">▭ Surlignage zone</button><button id="highlightText0914" type="button">T Surligner texte PDF</button></div><div class="highlightPresets0914"></div><div class="nlab0914Help">6 couleurs rapides + jusqu’à 6 couleurs personnelles. Maj+clic sur une couleur personnelle pour la retirer.</div>';picker.closest('.field,div')?.before(box);renderHighlightSwatches0914(box,picker);$14('highlightZone0914').onclick=()=>setEditorTool('highlight');$14('highlightText0914').onclick=()=>{setEditorTool('nativeText');st('Surligneur texte : sélectionnez précisément du texte PDF puis cliquez « Surligner ».')}}
// ---------------- Pen width numeric + reliable sync ----------------
function enhancePen0914(){const w=E.form?.querySelector('[name=penWidth]');if(!w||$14('penWidthNumber0914'))return;const div=document.createElement('div');div.className='penWidth0914';div.innerHTML='<span class="nlab0914Help">Épaisseur du trait</span><input id="penWidthNumber0914" type="number" min="0.5" max="18" step="0.5" value="'+safeHtml14(w.value||2)+'" title="Épaisseur en pixels">';w.closest('.rangeControl,.field,div')?.after(div);const n=$14('penWidthNumber0914');const syncFromSlider=()=>{n.value=w.value;syncSelected();syncPenUi();renderEditorOptions()};w.addEventListener('input',syncFromSlider);n.addEventListener('input',()=>{w.value=String(Math.max(.5,Math.min(18,+n.value||2)));w.dispatchEvent(new Event('input',{bubbles:true}))});}

// ---------------- Image rotation visible in sidebar ----------------
function enhanceImage0914(){const iw=E.form?.querySelector('[name=imageWidth]');if(!iw||$14('imageRotation0914'))return;const div=document.createElement('div');div.id='imageRotation0914';div.className='imageRotation0914';div.innerHTML='<button id="imgRotL0914" type="button" title="Rotation -90°">↺ 90°</button><button id="imgRotR0914" type="button" title="Rotation +90°">↻ 90°</button><input id="imgRotFree0914" type="number" min="-180" max="180" step="1" value="'+String(S.selectedAnn?.type==='image'?(S.selectedAnn.rotation||0):0)+'" title="Angle libre en degrés"><button id="imgRotApply0914" type="button">Appliquer</button>';iw.closest('.field,div')?.after(div);$14('imgRotL0914').onclick=()=>rotateSelectedImage(-90).catch(e=>st(e.message));$14('imgRotR0914').onclick=()=>rotateSelectedImage(90).catch(e=>st(e.message));$14('imgRotApply0914').onclick=()=>setSelectedImageRotation(+$14('imgRotFree0914').value||0).catch(e=>st(e.message));}

// ---------------- File preview / compact list ----------------
const VIEW_KEY0914='nlab-pdf-file-view-0914';let thumbUrls0914=[];
function viewPref0914(){try{return JSON.parse(localStorage.getItem(VIEW_KEY0914)||'{"mode":"list","preview":false}')}catch{return{mode:'li